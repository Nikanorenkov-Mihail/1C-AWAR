# AGENTS.md — Проект «1C-AWAR»

Инструкция для ИИ-ассистента по работе с кодом 1С. Обновляй этот файл при изменении
архитектуры или соглашений.

## 1. Назначение системы

Расширение конфигурации для **1С ERP Управление предприятием** (расширение, а не
самостоятельная база). Адаптация типовой ERP под нужды заказчика (ювелирное производство /
работа с драгоценными металлами).

Ключевая логика расширения:
- Учёт **драгоценных металлов** в номенклатуре и складских/производственных документах
  (лигатурный и чистый вес, процент содержания, движения по собственному регистру).
- Управление **производством**: виртуальные узлы ресурсных спецификаций и вспомогательные
  технологические операции (сворачивание полуфабрикатов в родительский этап).
- **Склад**: расчёт вместимости/заполненности складских ячеек по типовым упаковкам,
  нумерация ордеров.
- **Закупки**: значимость поставщиков, поставщики по товарным категориям/группам поставки,
  история изменений «план/факт».

## 2. Платформа и окружение

- Расширение конфигурации: `SB_Adaptation` (синоним «SB adaptation»)
- Назначение: `Customization` (доработка типовой)
- Префикс имён: `СБ_` (в части объектов встречается латинский `SB_` — см. п.6)
- Режим совместимости: `8.3.27`, управляемое приложение (Такси)
- Расширяемая конфигурация: 1С ERP Управление предприятием
- Используется БСП: **да** (через состав ERP; в коде применяются `ОбщегоНазначения.*`,
  `Перечисления.*`, штатные методы ERP)
- `KeepMappingToExtendedConfigurationObjectsByIDs = true` (привязка к объектам типовой по ID)

## 3. Архитектура

### Подсистемы
- `Закупки`
- `Склад`

### Собственные объекты (префикс `СБ_` / `SB_`)

**Общие модули** (главная бизнес-логика):
- `СБ_РаботаСДрагоценнымиМеталлами` — ядро учёта драгметаллов: формирование дерева/таблицы
  драгметаллов из таблицы товаров, расчёт лигатурного и чистого веса, проведение движений по
  регистру `СБ_ДвиженияДрагоценныхМеталлов`, получение серии номенклатуры.
- `SB_УправлениеПроизводствомВиртуальныеУзлы` — виртуальные узлы спецификаций и
  вспомогательные операции (ТЗ п.3.6): сворачивание состава узла в родительский этап,
  дополнение очереди производственных операций, перенумерация операций.
- `СБ_РаботаСДеревомЗначений` — утилиты по дереву значений: ключи строк/связей,
  конвертация дерево↔таблица, копирование/перенос строк, удаление дублей.
- `СБ_РасчетСкладскихЯчеек` — расчёт вместимости и заполненности складских ячеек
  (регистр `СБ_ВместимостьЯчеекПоТиповымУпаковкам` + остатки `ТоварыВЯчейках`).
- `СБ_НумерацияОрдеров` — подписки на события при установке нового номера приходного/
  расходного ордера (заглушки, логика не заполнена).

**Регистр накопления**:
- `СБ_ДвиженияДрагоценныхМеталлов` — ресурсы `ЧистыйВес`, `ЛигатурныйВес`;
  измерения `Номенклатура`, `НоменклатураРодитель`, `Характеристика`, `Серия`, `Склад`;
  реквизит `ЕдиницаИзмеренияВеса`.

**Регистры сведений**:
- `СБ_ПоставщикиПоТоварнымКатегориям`
- `СБ_ИсторияИзмененийПлан`, `СБ_ИсторияИзмененийФакт`
- `СБ_ВместимостьЯчеекПоТиповымУпаковкам`

**Справочники**:
- `СБ_КатегорииМатериалов`
- `СБ_Предопределенные` (команда `ОткрытьПредопределенные`)

**Отчёты**:
- `СБ_ПоставщикиПоГруппамПоставки`
- `СБ_ПланФакт`
- `СБ_ДвиженияДрагМеталлов`

### Доработанные типовые объекты ERP

В объекты типовой добавлены собственные реквизиты/табличные части:
- `Справочник.Номенклатура` — ТЧ `ДрагоценныеМатериалы` (состав драгметалла), реквизиты
  `СБ_ИспользоватьПроцентСодержания`, `СБ_ПроцентСодержания`, `КатегорияМатериала`.
- `Справочник.Партнеры` — `СБ_СтепеньЗначимостиПоставщика`, `СБ_ISO9001`, `СБ_IATF16949`.
- `Справочник.Склады` — `СБ_СкладНЗПИлиСкладСбыта`.
- `Справочник.ТехнологическиеОперации` — `SB_ВспомогательнаяОперация`.
- `Справочник.РесурсныеСпецификации` — ТЧ `МатериалыИУслуги.`реквизит `SB_ВиртуальныйУзел`.
- Документы (складские и производственные): `ВводОстатковТоваров`,
  `ВозвратТоваровПоставщику`, `ВнутреннееПотребление`, `ДвижениеПродукцииИМатериалов`,
  `ОприходованиеИзлишковТоваров`, `ЭтапПроизводства2_2`, `ЗаказНаПроизводство2_2`,
  `ПроизводственнаяОперация2_2`, `ПланПроизводства` и др. — добавлена ТЧ
  `СБ_ДрагоценныеМеталлы` (заполняется из `Товары`/`ОтгружаемыеТовары`).

### Роли
- `SB_ОсновнаяРоль`, `Расш1_ОсновнаяРоль`
- `АдминистраторСистемы`, `ЧтениеНормативноСправочнойИнформации`

## 4. Структура каталогов выгрузки

Код хранится в виде выгрузки конфигурации «в файлы» (Конфигуратор →
Конфигурация → Выгрузить конфигурацию в файлы).

**Важно:** актуальная выгрузка лежит во вложенном каталоге
`20260824\20260824\` (стандартная структура «папка выгрузки → Configuration.xml»).

```
20260824/20260824/Configuration.xml      ← корневое описание расширения
20260824/20260824/ConfigDumpInfo.xml     ← сопоставление объектов/версий
20260824/20260824/CommonModules/<Имя>/Ext/Module.bsl
20260824/20260824/Catalogs/<Имя>.xml
20260824/20260824/Catalogs/<Имя>/Ext/ObjectModule.bsl
20260824/20260824/Catalogs/<Имя>/Forms/<Форма>/Ext/Form/Module.bsl
20260824/20260824/Documents/<Имя>/...
20260824/20260824/InformationRegisters/<Имя>/...
20260824/20260824/AccumulationRegisters/<Имя>/...
20260824/20260824/Reports/<Имя>.xml
```

Правила:
- НЕ архивировать выгрузку (zip не индексируется).
- НЕ склеивать код в один файл.
- Один объект = один файл/каталог, как делает стандартная выгрузка.
- Код модулей хранится в `.bsl`, метаданные/формы — в `.xml`.

## 5. Точки входа и критичные модули

- **Проведение движений драгметаллов** — вызывается из `ОбработкаПроведения` объектных модулей
  документов (см. `Documents/*/Ext/ObjectModule.bsl`): обращение к
  `СБ_РаботаСДрагоценнымиМеталлами.ПроведениеДрагоценныеМеталлы(...)`.
- **Заполнение ТЧ «Драгоценные металлы»** — в формах документов:
  `СБ_РаботаСДрагоценнымиМеталлами.ЗаполнитьПустуюТаблицуДрагМеталлы(...)`.
- **Виртуальные узлы производства** — `SB_УправлениеПроизводствомВиртуальныеУзлы.СвернутьВиртуальныеУзлы(...)`,
  `ДополнитьОперацииУзлов(...)`; вызывается из модулей `Документы.*2_2` и `ОчередьПроизводственныхОпераций`.
- **Нумерация ордеров** — подписки `СБ_НумерацияОрдеров.СБ_ПодпискаНаСобытиеПриход/РасходПриУстановкеНовогоНомера(...)`.
- `Ext/ManagedApplicationModule.bsl` — пуст (нет логики в модуле управляемого приложения).

## 6. Соглашения по коду

- Префикс объектов и процедур/функций: `СБ_` (встречается и `SB_` латиницей — при новых
  правках придерживаться `СБ_`, латинский оставить как есть в уже существующем коде).
- Язык комментариев и имён: русский.
- Стиль имён процедур: `ГлаголСуществительное` (например, `СформироватьДеревоДрагоценныеМеталлы`,
  `ПолучитьСериюНоменклатуры`, `ЗаполнитьПустуюТаблицуДрагМеталлы`).
- Экспортируемые методы — с ключевым словом `Экспорт`; сервисные/внутренние выносятся в область
  `#Область СлужебныеПроцедурыИФункции` (образец — `SB_УправлениеПроизводствомВиртуальныеУзлы`).
- Обработка ошибок: `Попытка/Исключение` с сообщением через
  `ОбщегоНазначения.СообщитьПользователю(...)`.
- Комментарии к функциям оформляются блоками `//` с описанием «Параметры:» (примеры есть в
  `СБ_РаботаСДеревомЗначений` и `SB_УправлениеПроизводствомВиртуальныеУзлы`).
- Встречается много закомментированного «отладочного» кода — при правках не ориентироваться
  на него, опираться на рабочие участки.

## 7. Правила для ассистента

- **Если задача неоднозначна или данных недостаточно — задай уточняющие вопросы, не додумывай.**
  Используй инструмент `question` с конкретными вариантами (первым — рекомендуемый). Не
  придумывай типы объектов, имена методов, структуру ТЧ или бизнес-правила, которых нет в выгрузке.
- Перед поиском кода опирайся на структуру выгрузки из п.4 (корень — `20260824/20260824/`).
- Логика драгметаллов сосредоточена в `СБ_РаботаСДрагоценнымиМеталлами`; формы документов —
  тонкая обвязка вокруг неё.
- При правках учитывай соглашения из п.6 (префикс `СБ_`, русские имена, `ГлаголСуществительное`).
- Обновляй выгрузку после крупных изменений, чтобы индекс был актуальным.
- Доступные скилы (автозагрузка из `.opencode/skills/`):
  - `1c-code-check` — проверка синтаксиса BSL после генерации/правки кода (`check.mjs`);
  - `1c-architecture-graph` — граф зависимостей объектов (`graph.mjs`).
